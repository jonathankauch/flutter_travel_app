import 'package:flutter/material.dart';

const primaryColor = Color(0xFF3E4067);
const textColor = Color(0xFF3F4168);
const iconColor = Color(0xFF5E5E5E);

// SPACING
const defaultPadding = 20.0;

final boxShadow = BoxShadow(
    offset: Offset(5, 5),
    blurRadius: 10,
    color: Color(0xFFE9E9E9).withOpacity(0.50));
